{
  "name": "flashcards-open-source-app-aws",
  "version": "1.7.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "deploy": "cdk deploy --all",
    "destroy": "cdk destroy --all",
    "diff": "cdk diff",
    "synth": "cdk synth",
    "test": "npm run build && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@aws-crypto/client-node": "^5.0.0",
    "@aws-sdk/client-cloudwatch": "^3.1059.0",
    "@aws-sdk/client-s3": "^3.1059.0",
    "aws-cdk-lib": "^2.257.0",
    "constructs": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "aws-cdk": "^2.1125.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "uuid": "^11.1.1"
  },
  "engines": {
    "node": "24.x"
  }
}
